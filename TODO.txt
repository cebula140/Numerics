Numerics – TODO / znalezione problemy
========================================

--- KOMPILACJA (sypie się) ---

1. src/Calculus/Differentiation.cpp:9,20
   → "fabs" nie istnieje w C++ (to C-owska funkcja). Trzeba dać "std::abs".
   → Brakuje #include <cstdio> (printf) i #include <cstdlib> (exit).


--- BŁĘDY LOGICZNE W KODZIE ---

2. src/Calculus/Differentiation.cpp:11 – CentralDerivative
   → Wzór: (f(point+2h) + f(point-h)) / (2h)
   → To jest złe – central difference to (f(x+h) - f(x-h)) / (2h).
     Dodawanie zamiast odejmowania – wynik nie ma sensu.

3. src/Calculus/Differentiation.cpp:23 – FourPointDerivative
   → exit(1) przy złym accuracy – brutalne. Lepiej rzucić wyjątek (throw).

4. src/Calculus/RootFinding.cpp:5 – NewtonMethod
   → Zawsze zaczyna od x=1 (hardcoded: "f(1)"). Nie bierze początkowego
     przybliżenia z parametru.
   → Deklaracja w .hpp: NewtonMethod(const Function&, double accuracy = 4)
     – brak parametru "double x0", więc nie można wybrać startowego punktu.

5. include/Numerics/Calculus/RootFinding.hpp:5
   → AberthMethod jest zadeklarowane, ale NIE ZAIMPLEMENTOWANE w .cpp.
     Albo dopisać, albo usunąć deklarację.

6. src/Calculus/RootFinding.cpp:37
   → IsolateRoots zakomentowane – prawdopodobnie potrzebne do FindRoots,
     którego brak. main.cpp (wersja z testami macierzy) tego nie używa,
     ale jeśli chcesz FindRoots – trzeba dopisać.

7. src/Calculus/Integration.cpp:40-47 – make_key
   → Konwersja double → long long obcina precyzję (x * 1e9). Dla bardzo
     bliskich liczb może zwrócić ten sam klucz.

8. src/Calculus/Integration.cpp:51 – globalna zmienna cache
   → "my_map<double> cache" to zmienna globalna. Nie jest thread-safe,
     i trzyma wyniki między wywołaniami – może zdawać nieaktualne dane.


--- BŁĘDY W KLASIE Matrix ---

9. include/Numerics/Algebra/Matrix.hpp:9
   → data = my_dynamic_array<T>(rows * columns, 0)
   → Konstruktor my_dynamic_array(T init_value, ...?) ma błąd:
     w my_dynamic_array.hpp:41 konstruktor my_dynamic_array(T init_value)
     ustawia "size = capacity", a nie "size = 1". Przez to rows*columns
     jest ignorowane i tablica ma rozmiar = capacity = 2, a nie rows*cols!

10. include/Numerics/Algebra/Matrix.hpp:44 – operator*(T scalar)
    → Mnożenie przez skalar działa tylko jako "matrix * scalar".
      Brak "friend" wersji dla "scalar * matrix".

11. include/Numerics/Algebra/Matrix.hpp
    → Brak konstruktora przenoszącego (move) – kopiowanie przy zwracaniu
      z operator+/-/*.


--- BŁĘDY W DEPENDENCIACH ---

12. include/dependencies/my_dynamic_array.hpp:41-47
    → Konstruktor my_dynamic_array(T init_value) ustawia size = capacity,
      zamiast size = 1 i wypełnia pierwszy element. Przez to allocation
      ma rozmiar 2, ale size = 2 (wypełnione dwoma init_value).

13. include/dependencies/my_dynamic_array.hpp:15 – resize_arr
    → Nie obsługuje przypadku data == nullptr (gdy size == 0, capacity == 2,
      ale data może nie być zaalokowane, bo nie ma domyślnego konstruktora
      który inicjalizuje data). Właściwie jest konstruktor bezparametrowy,
      więc OK, ale ryzykowne.

14. include/dependencies/my_map.hpp:6
    → "using namespace std;" PRZED #pragma once – fatalne. Wciąga
      całą przestrzeń std do każdego pliku który includuje ten header.

15. include/dependencies/my_map.hpp:59-74 – rehash()
    → "data = my_dynamic_array<Slot>()" tworzy tablicę o capacity=2,
      a chwilę potem iteruje do capacity (które jest 512 po *= _capacity_factor)
      i robi insert_back. Tablica będzie się resize'owac w kółko (2→4→8→...→512),
      zamiast od razu zaalokować odpowiedni rozmiar.

16. include/dependencies/my_map.hpp:76 – maybe_reget_size()
    → Literówka ("reget" zamiast "rehash" / "maybe_rehash"). Działa, ale
      nazwa myląca.

17. include/dependencies/my_map.hpp:135 – operator[]
    → Operator[] automatycznie wstawia pustą wartość jeśli klucz nie istnieje.
      Brak const wersji, która by tego nie robiła (i rzucała błąd).


--- DROBNE / DESIGN ---

18. CMakeLists.txt
    → file(GLOB_RECURSE) – CMake nie lubi, nie śledzi nowych plików.
      Lepiej wymienić pliki ręcznie.
    → Brak target_link_libraries(numerics PRIVATE m) dla funkcji matematycznych.

19. app/main.cpp – #define M_PI
    → Nieprzenośne. W C++20 lepiej std::numbers::pi (potrzebne #include <numbers>).

20. app/main.cpp
    → main.cpp testuje tylko macierze. Kod dla Calculus (różniczkowanie,
      całkowanie, znajdowanie miejsc zerowych) nie jest testowany.

21. Ostrzeżenia:
    - Wszystkie .cpp: brak constexpr / noexcept gdzie mogłoby być
    - Brak testów jednostkowych
    - Brak dokumentacji co funkcje robią (opisy w .hpp)
    - exit(1) przy błędzie zamiast wyjątku – trudne do debugowania
